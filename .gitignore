.env
.venv/
__pycache__/
*.pyc
var/
*.sqlite3
.pytest_cache/
.DS_Store
logs/
config/secrets.json
config/cloudflared-config.yml

# Downloaded source files (OMI exports run to tens of MB and are not ours to
# redistribute). The drop folder is real; its contents are not committed.
data/
!data/.gitkeep
