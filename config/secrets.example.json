{
  "_comment": "Copy to config/secrets.json (gitignored). Same key names as trading-helper's, so the SMTP block can be pasted straight across. Anything left out here falls back to the uppercased name in .env.",

  "smtp_host": "smtp.gmail.com",
  "smtp_port": 587,
  "smtp_user": "you@example.com",
  "smtp_pass": "app-password-not-your-account-password",
  "smtp_from": "you@example.com",
  "digest_to": "you@example.com, spouse@example.com",
  "digest_bcc": "",

  "_imap_comment": "Only needed if eMoney exports arrive by email and you want them picked up automatically. A dedicated mailbox is better than your main one.",
  "imap_host": "imap.gmail.com",
  "imap_port": 993,
  "imap_user": "",
  "imap_pass": "",
  "imap_folder": "INBOX"
}
